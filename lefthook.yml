#pre-push:
#  parallel: true
#  commands:
#    static_code_analysis:
#      run: make lint

pre-commit:
  parallel: true
  scripts:
    "pre-commit.sh":
      runner: bash
#  commands:
#    format:
#      run: make format

commit-msg:
  scripts:
    "commit-msg.sh":
      runner: bash
